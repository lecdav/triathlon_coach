# Credentials — NE JAMAIS COMMITER
config/credentials.env

# Cache local (régénéré à chaque run, non versionné)
data/cache/

# data/today.json est versionné — c'est le seul fichier poussé par GitHub Actions

# Dashboard HTML local (contient données perso, non versionné)
reports/dashboard.html

# Rapports daily supprimés — non versionnés
reports/daily/

# OS / IDE
.DS_Store
.venv/
venv/
__pycache__/
*.pyc
